version: '3.8'

services:
  app:
    build: .
    container_name: finally-app
    ports:
      - "8000:8000"
    volumes:
      - finally-data:/app/db
    env_file:
      - .env
    restart: unless-stopped

volumes:
  finally-data:
    driver: local
